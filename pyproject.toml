[build-system]
requires = ["wheel", "setuptools"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 120
include = ".*pyi?$"
skip-magic-trailing-comma = true
target-version = ["py38"]

[tool.ruff]
line-length = 79
indent-width = 4

[tool.towncrier]
directory = "changes"